# GameReader v0.9.5 beta — dependencies
# Python 3.10+ required (3.11+ recommended)
# Windows only

# GUI
PySide6>=6.5.0
qtawesome>=1.3.0

# OCR
easyocr>=1.7.0
torch>=2.0.0

# Screen capture
mss>=9.0.0

# Image processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Audio playback
pygame>=2.5.0

# Audio ducking (Windows)
pycaw>=20240210

# Fuzzy matching
rapidfuzz>=3.0.0

# Process monitoring
psutil>=5.9.0

# Optional: pitch-preserving speed change (requires ffmpeg in resources/bin/)
pydub>=0.25.1

# Optional: thread limiting for parallel OCR workers
threadpoolctl>=3.1.0
